# Flash-Beeper

I needed something that can act as a random timer if I want to use the FLASH technique, a 'branch' of EMDR therapy.

## Running the project

1. Clone the project
2. Run `pnpm install`
3. Run `pnpm run dev`
